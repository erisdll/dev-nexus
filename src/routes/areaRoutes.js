const express = require('express');
const router = express.Router();
const areaController = require('../controllers/areaController');

// router.post('/areas-of-interest', areaController.createArea);
// router.get('/areas-of-interest', areaController.updateArea);
// router.patch('/areas-of-interest', areaController.updateArea);
// router.delete('/areas-of-interest', areaController.updateArea);

module.exports = router;
